#include <iostream>

using namespace std;

int main()
{
    system("chcp 1251");
    setlocale(LC_ALL, "rus");
    string name, lastName; 
    cout << "Введите имя: "; 
    cin >> name;  
    
    cout << "Введите фамилия: ";
    cin >> lastName;  
    cout << "Здраствуйте, " + name + " "  + lastName; 
}

